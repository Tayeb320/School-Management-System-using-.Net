@model SchoolWeb.Models.ExamTypes

@{
    ViewData["Title"] = "Create Exam";
}
<div class="container">

    <hr />
    <h2 class="crt">Add new Exam Type </h2>
    <hr />
    <div class="row">
        <div class="col-md-4">
            <form asp-action="CreateExam">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label class="control-label">Exam Type</label>
                    <input asp-for="ExamType_Name" class="form-control" />
                    <span asp-validation-for="ExamType_Name" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Create" class="MyBtn1" />
                </div>
            </form>
        </div>
    </div>

    <div>
        <a asp-action="ExamIndex">
            <input type="submit" value="Back to List" class="MyBtn" />
        </a>
    </div>

    @section Scripts {
        @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    }
</div>